{
  "buildCommand": "cd ../.. && pnpm turbo build --filter=@pod/dashboard",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
